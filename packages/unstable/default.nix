{ pkgs, ... }:

{
  # here are simple overrides copied from some version of unstable
  # exists because I don't want to pull down the entirity of unstable if I don't have to
  # redRocketTeam = pkgs.callPackage ./polymc {}; 
  # polymc = pkgs.callPackage ./polymc {};
}
